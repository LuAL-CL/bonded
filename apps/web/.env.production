# Loaded automatically by `next build` (NODE_ENV=production).
# These are safe defaults for serverless/demo deployments.
# Override in Vercel dashboard for production with real infrastructure.
DISABLE_JOBS=true
DEMO_MODE=true
NEXT_PUBLIC_DEMO_MODE=true
